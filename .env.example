# ===== COGNIFLUX environment =====
NODE_ENV=development
PORT=3100

# Database (PostgreSQL 16 - local)
DATABASE_URL=postgresql://cogniflux:CHANGE_ME@localhost:5432/cogniflux

# Redis (local)
REDIS_URL=redis://localhost:6379

# --- Provider credentials (fill what you have; providers without keys still work) ---
OPENALEX_API_KEY=
NCBI_API_KEY=
UNPAYWALL_EMAIL=

# Session / auth (generate: openssl rand -hex 32)
SESSION_SECRET=
