import {getDict,getServerLocale} from "@/lib/i18n-server";import {PrepTryoutClient} from "@/components/prep-tryout-client";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const t=await getDict(await getServerLocale());return <div className="max-w-5xl mx-auto px-4 py-10 pb-24"><h1 className="sr-only">{t.prep.tryoutTitle}</h1><PrepTryoutClient id={id}/></div>}
