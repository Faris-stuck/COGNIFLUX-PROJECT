import {getDict,getServerLocale} from "@/lib/i18n-server";import {PrepDashboard} from "@/components/prep-dashboard";export const dynamic="force-dynamic";export default async function Page(){const t=await getDict(await getServerLocale());return <div className="max-w-5xl mx-auto px-4 py-10 pb-24"><h1 className="text-3xl font-semibold tracking-tight">{t.prep.progressTitle}</h1><p className="mt-2 text-sm text-[var(--cf-text-muted)]">{t.prep.practiceSubtitle}</p><div className="mt-8"><PrepDashboard/></div></div>}
