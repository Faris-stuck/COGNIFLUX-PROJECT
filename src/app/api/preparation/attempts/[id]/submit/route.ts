import {NextRequest,NextResponse} from "next/server";import {submitAttempt} from "@/lib/prep-practice";export const dynamic="force-dynamic";export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const body=await req.json();const answers=body.answers&&typeof body.answers==='object'?body.answers:{};return NextResponse.json(await submitAttempt(id,answers))}catch(e){const code=e instanceof Error?e.message:'submit_failed';const allowed=new Set(['attempt_not_found','invalid_answer_option']);return NextResponse.json({error:allowed.has(code)?code:'submit_failed'},{status:400})}}
