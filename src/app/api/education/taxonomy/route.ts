import {NextResponse} from "next/server";import {getCms} from "@/lib/cms";export const dynamic="force-dynamic";export async function GET(){return NextResponse.json(await getCms("education.taxonomy","id"));}
